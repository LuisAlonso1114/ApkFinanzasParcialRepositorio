{
  "name": "balance-finanzas-estudiante",
  "version": "1.0.0",
  "description": "App de control financiero mensual para estudiantes, con alertas progresivas de presupuesto, registro por voz y personalización de interfaz.",
  "private": true,
  "scripts": {
    "start": "npx serve www -l 5173",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open:android": "npx cap open android",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
